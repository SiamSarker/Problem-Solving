#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *arr;
    int n;
    scanf("%d", &n);

    arr = (int*) malloc(sizeof(n*sizeof(int))); // type casting and memory malloc




    return 0;
}