#include "stdio.h"

void insert_char(char str[], char ch, int loc)
{
  
}

int main(int argc, char const *argv[]) {
    char str[100];
    char ch;
    int n;
    gets(str);
    scanf("%c", &ch);

  return 0;
}
